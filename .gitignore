# Segredos e configuração local (AD-7) — nunca commitados, só .env.example
.env
.env.*
!.env.example

# Chaves JWT (AD-2/RNF04) — nunca commitadas, geradas localmente e coladas no .env
*.pem

# Postgres local (docker-compose)
pgdata/

# Claude Code — config local, não é do projeto
.claude/

# BMAD: framework e config locais não vão pro repo. Artefatos de planejamento (_bmad-output) ficam versionados.
_bmad/

# OS/editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
